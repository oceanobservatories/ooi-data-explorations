# OOI Data Explorations with Julia

The four functions in this folder allow users to request and download OOI datasets via Julia.
Using the NCDatasets package, one can then pull data from remote OpenDAP or local NetCDFs.
Function descriptions can be found as comments in each function.